import { JobApplicationComment } from './job-application-comment';

describe('JobApplicationComment', () => {
  it('should create an instance', () => {
    expect(new JobApplicationComment()).toBeTruthy();
  });
});
